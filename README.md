# Getting Started with Run the server and the UI

## Available Scripts

In the project directory, you can run:

### `npm start` in to root
To run the server
### `npm start` in to client folder
To run the client part

